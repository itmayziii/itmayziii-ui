---
import NameBadge from './NameBadge.astro'
import Nav from './Nav.astro'

interface Props {
  fullName: string
}
const { fullName } = Astro.props<Props>;
---

<header class='sticky top-0 bg-background flex justify-between py-2 px-4 z-50'>
  <NameBadge fullName={fullName} />
  <Nav />
</header>
